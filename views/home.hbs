{{!-- views/home.hbs --}}

{{!-- Título principal de la sección --}}
<h2>Nuestros Productos Estrella</h2>
<p>Explora el catálogo de productos disponibles en nuestra tienda.</p>

<hr>

{{!-- 
  Aquí empieza la lógica de Handlebars.
  '#if' comprueba si la variable 'hayProductos' es verdadera.
  Esta variable la definimos en nuestro manager (productos.length > 0).
--}}
{{#if hayProductos}}
    <ul class="lista-productos">
        {{!-- 
          '#each' itera sobre cada elemento del array 'listaDeProductos'.
          Dentro de este bloque, puedes acceder a las propiedades de cada 
          producto usando 'this.propiedad' o simplemente 'propiedad'.
        --}}
        {{#each listaDeProductos}}
            <li>
                <strong>{{this.nombre}}</strong> - ${{this.precio}}

                {{!-- 
                  Usamos otro '#if' anidado para comprobar el estado del stock.
                  Si 'this.enStock' es verdadero, muestra el primer bloque.
                  Si no, el bloque 'else' se ejecuta.
                --}}
                {{#if this.enStock}}
                    <span class="stock-disponible">(Disponible)</span>
                {{else}}
                    <span class="stock-agotado">(Agotado)</span>
                {{/if}}
            </li>
        {{/each}}
    </ul>
{{else}}
    {{!-- 
      Este bloque 'else' se muestra si la condición '#if hayProductos' es falsa,
      es decir, si el array de productos estaba vacío.
    --}}
    <p>No hay productos para mostrar en este momento. Por favor, vuelve a intentarlo más tarde.</p>
{{/if}}